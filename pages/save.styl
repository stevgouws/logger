@import '../styles/variables.styl'

.mid
  flex 1
  display flex
  align-items center
  justify-content center
  margin $space-8

  .message
    font-size $3xl

  .data
    display grid
    grid-template-columns repeat(2, 1fr)
    font-size $3xl

    .value
      justify-self end
