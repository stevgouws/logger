import Layout from "../components/Layout";
import styles from "./done.styl";

const Done = () => {
  return (
    <Layout>
      <div className={styles.done}>
        <h1>DONE :)</h1>
      </div>
    </Layout>
  );
};

export default Done;
