@import '../styles/variables.styl'

.nextButton
  flex 1
  background-color $secondary
  color $white
  height $space-16
  display flex
  justify-content center
  align-items center
  font-size $4xl
  margin-top $space-8

  > *
    height 100%
    display flex
    align-items center
    justify-content center
    flex 1